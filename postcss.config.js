// Plain PostCSS config — no Tailwind, no extra plugins.
// This file intentionally overrides any existing postcss.config.js
// in a teammate's project that may have conflicting plugins (e.g. TailwindCSS).
export default {
  plugins: {},
}
